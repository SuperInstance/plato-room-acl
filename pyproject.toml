[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-room-acl"
version = "0.1.0"
description = "Room access control — role-based permissions (guest/member/mod/owner)"
license = {text = "MIT"}
requires-python = ">=3.8"
